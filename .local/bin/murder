#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

sub is_running {
    my ($pid) = @_;
    my $line_count = (`ps -p $pid` =~ tr/\n//);
    return $line_count == 2;
}

say is_running(1601)
