#!/usr/bin/env perl

use strict;
use warnings;

sub is_running {
    my ($pid) = @_;
    my $output = `ps -p $pid`;
    my $line_count = ($output =~ tr/\n//);
    return $line_count == 1;
}

print is_running(1602)
