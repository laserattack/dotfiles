#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

sub is_running {
    my ($pid) = @_;
    return `ps -p $pid` =~ tr/\n// == 2;
}

sub kill {
    my ($pid, $code) = @_;
    `kill -$code $pid`
}
