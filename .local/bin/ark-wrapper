#!/usr/bin/env bash

if pgrep -f 'perl.*ark'; then
    notify-send -t 3000 'ARK BACKUP' 'Already running'
elif $HOME/.local/bin/ark; then
    notify-send -t 5000 'ARK BACKUP' 'Done'
else
    notify-send -t 5000 'ARK BACKUP' 'Failed'
fi
