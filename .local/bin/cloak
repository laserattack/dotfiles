#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

sub cloakfolder {
}

sub cloakfile {
}

sub cloak {
    my ($arg) = @_;
    if (-f $arg) {
        cloakfile($arg);
    } elsif (-d $arg) {
        cloakfolder($arg);
    } else {
        die "[ERROR] '$arg' is not a file or dir\n"
    }
}

if (@ARGV < 1) {
    say 'cloak porno      # encrypt file/dir';
    say 'cloak porno.enc  # decrypt file/dir';
} else {
    foreach my $arg (@ARGV) {
        cloak($arg);
    }
}
